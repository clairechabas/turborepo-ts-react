{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "types-check": {
      "dependsOn": ["^types-check"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
